1. 根据目标，通过让 ai 不要猜测用户意图，而是提问的方式确认需求，并产出需求文档

---

目标
建立一个类似即梦的网站，功能只需要包括聊天生成图片
输入
当前工程是 web 前端工程，前端代码都生成在这里，采用前后端分离的方式
输出
请在 docs 目录下生成 requirement.md 文件，内容为需要实现这一目标的需求文档
步骤
我不了解需不需要实现后端，还是可以直接通过调用 openai 的 api 就可以实现想要的功能
不要猜测我的意图，当有不确定的点必须采用提问的方式和我确认

---

2. 根据需求文档，让 ai 不要猜测用户意图，而是提问的方式确认模块，并产出设计文档

---

目标
根据需求文档生成功能设计文档
输入
docs/requirement.md
输出
docs/design.md
步骤
根据需求文档，划分模块，识别模块之间的关系，每个模块需要可以单独测试
不要猜测我的意图，有不清楚的地方必须和我确认

---

3. 根据需求文档和设计文档拆分/细化任务，并输出到任务文件中，同时建立 progress.md 记录任务完成情况

---

目标
根据需求文档和设计文档生成详细的任务列表和任务进度
输入
/docs/requirement.md
/docs/design.md
输出
/docs/tasks/<task-name>.md
/docs/tasks/progress.md
步骤
扫描需求文档和设计文档，拆解任务，并写入到各自的任务说明文件里
任务进度保存在 progress.md 文件中，记录有哪些任务，每个任务的完成情况

---

4. 根据需求文档、设计文档、tasks，让 ai 生成 vibe coding 的 prompt 文档（可选）

---

目标
根据需求文档、设计文档和 tasks/生成每个任务执行时的 prompt
输入
/docs/requirement.md
/docs/design.md
/docs/tasks/
输出
/docs/prompt.md
步骤
根据 tasks，生成每个任务完成所需要的 prompt 提示词
每个任务的提示词独立，通过 progress.md 管控模块之间的联系

---

5. 根据文档开始生成项目

---

目标
生成具体代码
输入
/docs/requirement.md
/docs/design.md
/docs/tasks
/docs/prompt.md
输出
具体目录和代码
步骤
为避免任务模块太多导致上下文丢失，每个任务创建一个子 agent 执行，父 agent 管控任务的执行情况和关联
根据需求文档和设计文档以及任务划分，建立前后端文件夹并生成代码文件
生成单元测试代码，务必保证代码都通过单元测试
有任何不确定的问题必须向我确认

---
